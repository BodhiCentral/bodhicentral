# Untitled UI v7.0 to v8.0 Upgrade Instructions

This file was generated by `untitledui upgrade`. It is meant to guide
manual review of changes that could not be fully automated.

**Important**: Ignore pre-existing type errors and linting issues.
Focus only on migration-related changes listed below.

Delete this file after review is complete.

## Manual review items

These class replacements may need manual verification:

### src/components/application/date-picker/cell.tsx:50

bg-active → bg-primary_hover

```diff
- isInRange && isDisabled && "bg-active",
+ isInRange && isDisabled && "bg-primary_hover",
```

### src/components/application/date-picker/cell.tsx:51

bg-active → bg-primary_hover

```diff
- isSelected && isRangeCalendar && "bg-active",
+ isSelected && isRangeCalendar && "bg-primary_hover",
```

### src/components/application/date-picker/cell.tsx:61

bg-active → bg-primary_hover

```diff
- "after:absolute after:inset-0 after:translate-x-full after:bg-gradient-to-l after:from-transparent after:to-bg-active in-[[role=gridcell]:last-child]:after:hidden",
+ "after:absolute after:inset-0 after:translate-x-full after:bg-gradient-to-l after:from-transparent after:to-bg-primary_hover in-[[role=gridcell]:last-child]:after:hidden",
```

### src/components/application/date-picker/cell.tsx:68

bg-active → bg-primary_hover

```diff
- "after:absolute after:inset-0 after:-translate-x-full after:bg-gradient-to-r after:from-transparent after:to-bg-active in-[[role=gridcell]:first-child]:after:hidden",
+ "after:absolute after:inset-0 after:-translate-x-full after:bg-gradient-to-r after:from-transparent after:to-bg-primary_hover in-[[role=gridcell]:first-child]:after:hidden",
```

### src/components/application/date-picker/cell.tsx:80

text-disabled → removed

```diff
- isDisabled ? "text-disabled" : "text-secondary hover:text-secondary_hover",
+ isDisabled ? "" : "text-secondary hover:text-secondary_hover",
```

### src/components/application/date-picker/cell.tsx:88

bg-active → bg-primary_hover

```diff
- !isSelected && isTodayDate ? "bg-active font-medium hover:bg-secondary_hover" : "",
+ !isSelected && isTodayDate ? "bg-primary_hover font-medium hover:bg-secondary_hover" : "",
```

### src/components/application/date-picker/range-preset.tsx:21

bg-active → bg-primary_hover

```diff
- isSelected ? "bg-active text-secondary_hover hover:bg-secondary_hover" : "text-secondary hover:bg-primary_hover hover:text-secondary_hover",
+ isSelected ? "bg-primary_hover text-secondary_hover hover:bg-secondary_hover" : "text-secondary hover:bg-primary_hover hover:text-secondary_hover",
```

### src/components/application/file-upload/file-upload-base.tsx:206

bg-disabled_subtle → opacity-50; ring-disabled_subtle → removed

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle ring-disabled_subtle",
+ isDisabled && "cursor-not-allowed opacity-50 ",
```

### src/components/application/tabs/tabs.tsx:29

bg-active → bg-primary_hover

```diff
- isSelected && "bg-active text-secondary",
+ isSelected && "bg-primary_hover text-secondary",
```

### src/components/base/button-group/button-group.tsx:20

text-disabled → removed

```diff
- "disabled:cursor-not-allowed disabled:bg-primary disabled:text-disabled",
+ "disabled:cursor-not-allowed disabled:bg-primary disabled:",
```

### src/components/base/button-group/button-group.tsx:22

bg-disabled_subtle → opacity-50; bg-active → bg-primary_hover

```diff
- "selected:bg-active selected:text-secondary_hover selected:disabled:bg-disabled_subtle",
+ "selected:bg-primary_hover selected:text-secondary_hover selected:disabled:opacity-50",
```

### src/components/base/button-group/button-group.tsx:24

text-fg-disabled_subtle → removed

```diff
- icon: "pointer-events-none text-fg-quaternary transition-[inherit] group-hover/button-group:text-fg-quaternary_hover group-disabled/button-group:text-fg-disabled_subtle",
+ icon: "pointer-events-none text-fg-quaternary transition-[inherit] group-hover/button-group:text-fg-quaternary_hover group-disabled/button-group:",
```

### src/components/base/buttons/button-utility.tsx:14

shadow-xs-skeumorphic → shadow-xs-skeuomorphic (typo fix); ring-disabled_subtle → removed

```diff
- "bg-primary text-fg-quaternary shadow-xs-skeumorphic ring-1 ring-primary ring-inset hover:bg-primary_hover hover:text-fg-quaternary_hover disabled:shadow-xs disabled:ring-disabled_subtle",
+ "bg-primary text-fg-quaternary shadow-xs-skeuomorphic ring-1 ring-primary ring-inset hover:bg-primary_hover hover:text-fg-quaternary_hover disabled:shadow-xs disabled:",
```

### src/components/base/buttons/button-utility.tsx:92

text-fg-disabled_subtle → removed

```diff
- "group relative inline-flex h-max cursor-pointer items-center justify-center rounded-md p-1.5 outline-focus-ring transition duration-100 ease-linear focus-visible:outline-2 focus-visible:outline-offset-2 disabled:cursor-not-allowed disabled:text-fg-disabled_subtle",
+ "group relative inline-flex h-max cursor-pointer items-center justify-center rounded-md p-1.5 outline-focus-ring transition duration-100 ease-linear focus-visible:outline-2 focus-visible:outline-offset-2 disabled:cursor-not-allowed disabled:",
```

### src/components/base/buttons/button.tsx:17

text-fg-disabled → removed

```diff
- "disabled:cursor-not-allowed disabled:text-fg-disabled",
+ "disabled:cursor-not-allowed disabled:",
```

### src/components/base/buttons/button.tsx:19

text-fg-disabled_subtle → removed

```diff
- "disabled:*:data-icon:text-fg-disabled_subtle",
+ "disabled:*:data-icon:",
```

### src/components/base/buttons/button.tsx:57

bg-disabled → opacity-50; ring-disabled_subtle → removed

```diff
- "disabled:bg-disabled disabled:shadow-xs disabled:ring-disabled_subtle",
+ "disabled:opacity-50 disabled:shadow-xs disabled:",
```

### src/components/base/buttons/button.tsx:66

ring-disabled_subtle → removed

```diff
- "disabled:shadow-xs disabled:ring-disabled_subtle",
+ "disabled:shadow-xs disabled:",
```

### src/components/base/buttons/button.tsx:102

bg-disabled → opacity-50; ring-disabled_subtle → removed

```diff
- "disabled:bg-disabled disabled:shadow-xs disabled:ring-disabled_subtle",
+ "disabled:opacity-50 disabled:shadow-xs disabled:",
```

### src/components/base/buttons/button.tsx:111

ring-disabled_subtle → removed

```diff
- "disabled:bg-primary disabled:shadow-xs disabled:ring-disabled_subtle",
+ "disabled:bg-primary disabled:shadow-xs disabled:",
```

### src/components/base/buttons/social-button.tsx:11

text-fg-disabled → removed

```diff
- root: "group relative inline-flex h-max cursor-pointer items-center justify-center font-semibold whitespace-nowrap outline-focus-ring transition duration-100 ease-linear before:absolute focus-visible:outline-2 focus-visible:outline-offset-2 disabled:cursor-not-allowed disabled:stroke-fg-disabled disabled:text-fg-disabled disabled:*:text-fg-disabled",
+ root: "group relative inline-flex h-max cursor-pointer items-center justify-center font-semibold whitespace-nowrap outline-focus-ring transition duration-100 ease-linear before:absolute focus-visible:outline-2 focus-visible:outline-offset-2 disabled:cursor-not-allowed disabled:stroke-fg-disabled disabled: disabled:*:",
```

### src/components/base/checkbox/checkbox.tsx:23

bg-disabled_subtle → opacity-50; ring-disabled → removed

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle ring-disabled",
+ isDisabled && "cursor-not-allowed opacity-50 ",
```

### src/components/base/checkbox/checkbox.tsx:36

text-fg-disabled_subtle → removed

```diff
- isDisabled && "text-fg-disabled_subtle",
+ isDisabled && "",
```

### src/components/base/checkbox/checkbox.tsx:50

text-fg-disabled_subtle → removed

```diff
- isDisabled && "text-fg-disabled_subtle",
+ isDisabled && "",
```

### src/components/base/dropdown/dropdown.tsx:63

text-fg-disabled → removed

```diff
- className={cx("mr-2 size-4 shrink-0 stroke-[2.25px]", state.isDisabled ? "text-fg-disabled" : "text-fg-quaternary")}
+ className={cx("mr-2 size-4 shrink-0 stroke-[2.25px]", state.isDisabled ? "" : "text-fg-quaternary")}
```

### src/components/base/dropdown/dropdown.tsx:70

text-disabled → removed

```diff
- state.isDisabled ? "text-disabled" : "text-secondary",
+ state.isDisabled ? "" : "text-secondary",
```

### src/components/base/dropdown/dropdown.tsx:81

text-disabled → removed

```diff
- state.isDisabled ? "text-disabled" : "text-quaternary",
+ state.isDisabled ? "" : "text-quaternary",
```

### src/components/base/input/input-group.tsx:30

bg-disabled_subtle → opacity-50; border-disabled → removed

```diff
- isDisabled && "border-disabled bg-disabled_subtle text-tertiary",
+ isDisabled && " opacity-50 text-tertiary",
```

### src/components/base/input/input-group.tsx:31

bg-disabled_subtle → opacity-50; text-disabled → removed; border-disabled → removed

```diff
- "in-data-input-wrapper:group-disabled:bg-disabled_subtle in-data-input-wrapper:group-disabled:text-disabled in-data-input-wrapper:group-disabled:ring-border-disabled",
+ "in-data-input-wrapper:group-disabled:opacity-50 in-data-input-wrapper:group-disabled: in-data-input-wrapper:group-disabled:ring-",
```

### src/components/base/input/input-group.tsx:107

bg-disabled_subtle → opacity-50; border-disabled → removed

```diff
- isDisabled && "cursor-not-allowed has-[&>select]:bg-disabled_subtle has-[&>select]:ring-border-disabled",
+ isDisabled && "cursor-not-allowed has-[&>select]:opacity-50 has-[&>select]:ring-",
```

### src/components/base/input/input-group.tsx:115

text-disabled → removed

```diff
- <p className={cx("text-md text-tertiary", isDisabled && "text-disabled")}>{prefix}</p>
+ <p className={cx("text-md text-tertiary", isDisabled && "")}>{prefix}</p>
```

### src/components/base/input/input.tsx:91

bg-disabled_subtle → opacity-50; ring-disabled → removed

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle ring-disabled",
+ isDisabled && "cursor-not-allowed opacity-50 ",
```

### src/components/base/input/input.tsx:92

bg-disabled_subtle → opacity-50; ring-disabled → removed

```diff
- "group-disabled:cursor-not-allowed group-disabled:bg-disabled_subtle group-disabled:ring-disabled",
+ "group-disabled:cursor-not-allowed group-disabled:opacity-50 group-disabled:",
```

### src/components/base/input/input.tsx:112

text-fg-disabled → removed

```diff
- isDisabled && "text-fg-disabled",
+ isDisabled && "",
```

### src/components/base/input/input.tsx:127

text-disabled → removed

```diff
- isDisabled && "cursor-not-allowed text-disabled",
+ isDisabled && "cursor-not-allowed ",
```

### src/components/base/input/input.tsx:173

text-disabled → removed

```diff
- isDisabled && "bg-transparent text-disabled",
+ isDisabled && "bg-transparent ",
```

### src/components/base/pin-input/pin-input.tsx:93

text-placeholder_subtle → removed

```diff
- "relative flex items-center justify-center rounded-xl bg-primary text-center text-placeholder_subtle shadow-xs ring-1 ring-primary transition-[box-shadow,background-color] duration-100 ease-linear ring-inset",
+ "relative flex items-center justify-center rounded-xl bg-primary text-center  shadow-xs ring-1 ring-primary transition-[box-shadow,background-color] duration-100 ease-linear ring-inset",
```

### src/components/base/pin-input/pin-input.tsx:97

bg-disabled_subtle → opacity-50; ring-disabled → removed; text-fg-disabled_subtle → removed

```diff
- disabled && "bg-disabled_subtle text-fg-disabled_subtle ring-disabled",
+ disabled && "opacity-50  ",
```

### src/components/base/pin-input/pin-input.tsx:120

text-placeholder_subtle → removed

```diff
- <div role="separator" {...props} className={cx("text-center text-display-xl font-medium text-placeholder_subtle", props.className)}>
+ <div role="separator" {...props} className={cx("text-center text-display-xl font-medium ", props.className)}>
```

### src/components/base/select/combobox.tsx:48

bg-disabled_subtle → opacity-50

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle",
+ isDisabled && "cursor-not-allowed opacity-50",
```

### src/components/base/select/combobox.tsx:61

text-disabled → removed

```diff
- <p className={cx("text-md font-medium text-primary", isDisabled && "text-disabled")}>{first}</p>
+ <p className={cx("text-md font-medium text-primary", isDisabled && "")}>{first}</p>
```

### src/components/base/select/combobox.tsx:62

text-disabled → removed

```diff
- {last && <p className={cx("-ml-0.75 text-md text-tertiary", isDisabled && "text-disabled")}>{last}</p>}
+ {last && <p className={cx("-ml-0.75 text-md text-tertiary", isDisabled && "")}>{last}</p>}
```

### src/components/base/select/combobox.tsx:68

text-disabled → removed

```diff
- className="z-10 w-full appearance-none bg-transparent text-md text-transparent caret-alpha-black/90 placeholder:text-placeholder focus:outline-hidden disabled:cursor-not-allowed disabled:text-disabled disabled:placeholder:text-disabled"
+ className="z-10 w-full appearance-none bg-transparent text-md text-transparent caret-alpha-black/90 placeholder:text-placeholder focus:outline-hidden disabled:cursor-not-allowed disabled: disabled:placeholder:"
```

### src/components/base/select/combobox.tsx:76

bg-disabled_subtle → opacity-50

```diff
- isDisabled && "to-bg-disabled_subtle",
+ isDisabled && "to-opacity-50",
```

### src/components/base/select/combobox.tsx:84

text-disabled → removed

```diff
- isDisabled && "bg-transparent text-disabled",
+ isDisabled && "bg-transparent ",
```

### src/components/base/select/select-item.tsx:48

bg-active → bg-primary_hover

```diff
- state.isSelected && "bg-active",
+ state.isSelected && "bg-primary_hover",
```

### src/components/base/select/select-item.tsx:55

text-fg-disabled → removed

```diff
- state.isDisabled && "*:data-icon:text-fg-disabled",
+ state.isDisabled && "*:data-icon:",
```

### src/components/base/select/select-item.tsx:71

text-disabled → removed

```diff
- className={cx("truncate text-md font-medium whitespace-nowrap text-primary", state.isDisabled && "text-disabled")}
+ className={cx("truncate text-md font-medium whitespace-nowrap text-primary", state.isDisabled && "")}
```

### src/components/base/select/select-item.tsx:77

text-disabled → removed

```diff
- <AriaText slot="description" className={cx("text-md whitespace-nowrap text-tertiary", state.isDisabled && "text-disabled")}>
+ <AriaText slot="description" className={cx("text-md whitespace-nowrap text-tertiary", state.isDisabled && "")}>
```

### src/components/base/select/select-item.tsx:89

text-fg-disabled → removed

```diff
- state.isDisabled && "text-fg-disabled",
+ state.isDisabled && "",
```

### src/components/base/select/select-native.tsx:36

bg-disabled_subtle → opacity-50; text-disabled → removed

```diff
- "appearance-none rounded-lg bg-primary px-3.5 py-2.5 text-md font-medium text-primary shadow-xs ring-1 ring-primary outline-hidden transition duration-100 ease-linear ring-inset placeholder:text-fg-quaternary focus-visible:ring-2 focus-visible:ring-brand disabled:cursor-not-allowed disabled:bg-disabled_subtle disabled:text-disabled",
+ "appearance-none rounded-lg bg-primary px-3.5 py-2.5 text-md font-medium text-primary shadow-xs ring-1 ring-primary outline-hidden transition duration-100 ease-linear ring-inset placeholder:text-fg-quaternary focus-visible:ring-2 focus-visible:ring-brand disabled:cursor-not-allowed disabled:opacity-50 disabled:",
```

### src/components/base/select/select-native.tsx:40

text-disabled → removed

```diff
- "in-data-input-wrapper:group-disabled:pointer-events-none in-data-input-wrapper:group-disabled:cursor-not-allowed in-data-input-wrapper:group-disabled:bg-transparent in-data-input-wrapper:group-disabled:text-disabled",
+ "in-data-input-wrapper:group-disabled:pointer-events-none in-data-input-wrapper:group-disabled:cursor-not-allowed in-data-input-wrapper:group-disabled:bg-transparent in-data-input-wrapper:group-disabled:",
```

### src/components/base/select/select.tsx:63

bg-disabled_subtle → opacity-50; text-disabled → removed

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle text-disabled",
+ isDisabled && "cursor-not-allowed opacity-50 ",
```

### src/components/base/select/select.tsx:71

text-fg-disabled → removed

```diff
- "*:data-icon:size-5 *:data-icon:shrink-0 *:data-icon:text-fg-quaternary in-disabled:*:data-icon:text-fg-disabled",
+ "*:data-icon:size-5 *:data-icon:shrink-0 *:data-icon:text-fg-quaternary in-disabled:*:data-icon:",
```

### src/components/base/select/select.tsx:94

text-disabled → removed

```diff
- <p className={cx("text-md text-placeholder", isDisabled && "text-disabled")}>{placeholder}</p>
+ <p className={cx("text-md text-placeholder", isDisabled && "")}>{placeholder}</p>
```

### src/components/base/select/tag-select.tsx:291

outline-none → outline-hidden; text-disabled → removed

```diff
- className="w-full flex-[1_0_0] appearance-none bg-transparent text-md text-ellipsis text-primary caret-alpha-black/90 outline-none placeholder:text-placeholder focus:outline-hidden disabled:cursor-not-allowed disabled:text-disabled disabled:placeholder:text-disabled"
+ className="w-full flex-[1_0_0] appearance-none bg-transparent text-md text-ellipsis text-primary caret-alpha-black/90 outline-hidden placeholder:text-placeholder focus:outline-hidden disabled:cursor-not-allowed disabled: disabled:placeholder:"
```

### src/components/base/select/tag-select.tsx:305

text-disabled → removed

```diff
- isDisabled && "bg-transparent text-disabled",
+ isDisabled && "bg-transparent ",
```

### src/components/base/select/tag-select.tsx:333

bg-disabled_subtle → opacity-50

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle",
+ isDisabled && "cursor-not-allowed opacity-50",
```

### src/components/base/radio-buttons/radio-buttons.tsx:33

bg-disabled_subtle → opacity-50; border-disabled → removed

```diff
- isDisabled && "cursor-not-allowed border-disabled bg-disabled_subtle",
+ isDisabled && "cursor-not-allowed  opacity-50",
```

### src/components/base/textarea/textarea.tsx:38

bg-disabled_subtle → opacity-50; text-disabled → removed; ring-disabled → removed

```diff
- state.isDisabled && "cursor-not-allowed bg-disabled_subtle text-disabled ring-disabled",
+ state.isDisabled && "cursor-not-allowed opacity-50  ",
```

### src/components/base/toggle/toggle.tsx:50

bg-disabled → opacity-50

```diff
- isDisabled && "cursor-not-allowed bg-disabled",
+ isDisabled && "cursor-not-allowed opacity-50",
```

### src/components/base/toggle/toggle.tsx:65

bg-toggle-button-fg_disabled → removed

```diff
- isDisabled && "bg-toggle-button-fg_disabled",
+ isDisabled && "",
```

### src/components/application/app-navigation/base-components/nav-button.tsx:59

bg-active → bg-primary_hover

```diff
- current && "bg-active text-fg-quaternary_hover hover:bg-secondary_hover",
+ current && "bg-primary_hover text-fg-quaternary_hover hover:bg-secondary_hover",
```

### src/components/application/app-navigation/base-components/nav-item.tsx:11

bg-active → bg-primary_hover

```diff
- rootSelected: "bg-active hover:bg-secondary_hover",
+ rootSelected: "bg-primary_hover hover:bg-secondary_hover",
```

### src/components/base/tags/base-components/tag-checkbox.tsx:22

bg-disabled_subtle → opacity-50; ring-disabled → removed

```diff
- isDisabled && "cursor-not-allowed bg-disabled_subtle ring-disabled",
+ isDisabled && "cursor-not-allowed opacity-50 ",
```

### src/components/base/tags/base-components/tag-checkbox.tsx:37

text-fg-disabled_subtle → removed

```diff
- isDisabled && "text-fg-disabled_subtle",
+ isDisabled && "",
```

## Additional notes

- Default size for Select, ComboBox, and Input changed from 'sm' to 'md'. Review component usage.
- Avatar size 'xxs' removed. Use 'xs' instead.
- Dropdown no longer defaults to selectionMode='single'. Set explicitly if needed.

